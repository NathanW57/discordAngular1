import { UpdateUserPipe } from './update-user.pipe';

describe('UpdateUserPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateUserPipe();
    expect(pipe).toBeTruthy();
  });
});
